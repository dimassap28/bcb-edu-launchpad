/** Ikon mengambang di hero section modul absensi */
export interface FloatingIcon {
  emoji: string;
  label: string;
  x: string;
  y: string;
}

/** Fitur / highlight sebuah modul */
export interface ModuleFeature {
  emoji: string;
  title: string;
  desc: string;
}

/** CSS class helper untuk card hover */
export type CardHoverClass = string;
